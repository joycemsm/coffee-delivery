import styled from "styled-components"

export const LayoutContainer = styled.div`
  margin: 5rem auto;
  padding: 2.5rem;
  display: flex;
  flex-direction: row;
  overflow: hidden;
  flex-wrap: wrap;
`